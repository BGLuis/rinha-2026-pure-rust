[package]
name = "rust_api"
version = "0.1.0"
edition = "2021"

[dependencies]
rust_engine = { path = "../engine" }
libc = "0.2"

[profile.release]
opt-level = 3
lto = "fat"
codegen-units = 1
panic = "abort"
